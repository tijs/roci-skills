---
name: server-version
description: Check VPS system information and versions. Use when asked about server specs, Node/Python versions, kernel info, or system health overview.
---

# Server Version Check

Run the check script to gather all system version information:

```bash
bash /home/tijs/roci/skills/server-version/scripts/check.sh
```

The script outputs:
- Kernel version
- Node.js version
- Python version
- OS release info

Report the results in a clean summary.
