---
name: uptime
description: Check server uptime and load averages
tools_required: [Bash]
---

# Server Uptime Check

Run the following commands:

1. Run `uptime` to see how long the server has been running and current load averages
2. Run `free -h` to check memory usage
3. Run `df -h /` to check disk usage on root partition

Report the results with any concerns about resource usage.
